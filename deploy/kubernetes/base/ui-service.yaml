apiVersion: v1
kind: Service
metadata:
  name: useful-cookery-ui
  labels:
    app: useful-cookery
    component: ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: useful-cookery
    component: ui
