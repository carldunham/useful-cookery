apiVersion: v1
kind: Service
metadata:
  name: useful-cookery-api
  labels:
    app: useful-cookery
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: useful-cookery
    component: api
